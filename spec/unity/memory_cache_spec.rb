# frozen_string_literal: true

RSpec.describe Unity::MemoryCache do
  it "has a version number" do
    expect(Unity::MemoryCache::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
